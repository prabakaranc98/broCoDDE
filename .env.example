# ─────────────────────────────────────────────────────────────────
# BroCoDDE — Environment Configuration
# Copy to .env and fill in your actual values.
# ─────────────────────────────────────────────────────────────────

# ── Primary AI Provider: OpenRouter ──────────────────────────────
# Single key for all models: Claude, GPT, Qwen, Gemini, etc.
# Get your key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...

# ── Model Tier Overrides (optional) ──────────────────────────────
# Leave blank to use router.py defaults (recommended for most setups).
# Override with any OpenRouter model path if you want specific models.
#
# Tier defaults (from router.py):
#   Tier 1 → openai/gpt-5.2-chat         (grammar, memory writes, utility)
#   Tier 2 → anthropic/claude-sonnet-4.6  (extraction, lint, structuring, Q&A)
#   Tier 3 → anthropic/claude-opus-4.6    (discovery briefs, post-mortem, deep critique)
#
# Example overrides:
#   TIER2_MODEL=openai/gpt-5.2
#   TIER3_MODEL=google/gemini-3.1-pro
TIER1_MODEL=
TIER2_MODEL=
TIER3_MODEL=

# ── Search: Exa API ───────────────────────────────────────────────
# Used by Strategist during Discovery to surface trending topics,
# recent papers, and platform conversations.
# Get your key at https://exa.ai
EXA_API_KEY=exa-...

# ── Database ──────────────────────────────────────────────────────
# SQLite for local development (zero config)
DATABASE_URL=sqlite+aiosqlite:///./brocodde.db
# PostgreSQL for production (via Supabase or self-hosted)
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/brocodde

# ── Application ───────────────────────────────────────────────────
CORS_ORIGINS=http://localhost:3000
SECRET_KEY=change-me-in-production
ENVIRONMENT=development
LOG_LEVEL=INFO

# ── Frontend ──────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_VERSION=0.4.0
